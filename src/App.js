import React from 'react';
import './App.css';
import './timedate.css';
import './changindate.css';
import backgroundtop from './assets/images/background-top.png';
import Button from './component/Button';

function App() {
  return (
    <div className="time-website">
      <img src={backgroundtop} className="backgroundImage" />
      <div className="time-website-boxs ">
        <div className="time-website-time-top">
          <div className="time-website-time-top-inner">jooihnubujb</div>
        </div>
        <div className="time-website-time">
          <Button />
        </div>
        <div>
          This establishes the main-axis, thus defining the direction flex items
          are placed in the flex container. Flexbox is (aside from optional
          wrapping) a single-direction layout concept. Think of flex items as
          primarily laying out either in horizontal rows or vertical columns.
        </div>

        <div className="time-website-date">
          edgbjlergjmeThis establishes the main-axis, thus defining the dir
        </div>
      </div>
      <div className="time-website-boxs ">
        edgbjlergjmeThis establishes the main-axis, thus defining the direction
        flex items are placed in the flex container. Flexbox is (aside from
        optional wrapping) a single-direction layout concept. Think of flex
        items as primarily laying out either in horizontal rows or vertical
        columns.r
      </div>
      <div className="time-website-boxs ">
        edgbjlergjmeThis establishes the main-axis, thus defining the direction
        flex items are placed in the flex container. Flexbox is (aside from
        optional wrapping) a single-direction layout concept. Think of flex
        items as primarily laying out either in horizontal rows or vertical
        columns.r
      </div>
      <div className="time-website-boxs ">
        edgbjlergjmeThis establishes the main-axis, thus defining the direction
        flex items are placed in the flex container. Flexbox is (aside from
        optional wrapping) a single-direction layout concept. Think of flex
        items as primarily laying out either in horizontal rows or vertical
        columns.r
      </div>
    </div>
  );
}

export default App;
